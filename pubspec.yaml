name: tech_world
description: "A world full of tech and adventures."
publish_to: 'none'
version: 0.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firedart:
    git:
      url: https://github.com/nickmeinhold/firedart.git
      ref: notify-auth-state-on-listen
  livekit_client: ^2.2.0
  shared_preferences: ^2.2.3
  flutter_webrtc: ^0.10.8
  collection: ^1.18.0
  flame: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  assets:
    - assets/images/